// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://you-saku.github.io/japanese-it-conferences-with-support',
});
